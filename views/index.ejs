<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Office Management System</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #6a11cb, #2575fc);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-family: 'Segoe UI', sans-serif;
    }
    .card {
      border: none;
      border-radius: 20px;
      box-shadow: 0 6px 15px rgba(0,0,0,0.3);
      transition: transform 0.3s;
    }
    .card:hover {
      transform: translateY(-8px);
    }
    .dashboard {
      max-width: 1000px;
    }
    .stats-card {
      background: rgba(255,255,255,0.2);
      border-radius: 15px;
      padding: 20px;
      color: #fff;
      backdrop-filter: blur(10px);
      margin-bottom: 30px;
    }
  </style>
</head>
<body>
  <div class="container dashboard text-center">
    <h1 class="mb-4 fw-bold">🏢 Office Management System</h1>
    <p class="lead mb-5">Manage your Departments and Employees efficiently</p>

    <!-- Live Stats -->
    <div class="row mb-5">
      <div class="col-md-6">
        <div class="stats-card">
          <h2><%= departmentCount %></h2>
          <p>Total Departments</p>
        </div>
      </div>
      <div class="col-md-6">
        <div class="stats-card">
          <h2><%= employeeCount %></h2>
          <p>Total Employees</p>
        </div>
      </div>
    </div>

    <!-- Navigation Cards -->
    <div class="row g-4">
      <div class="col-md-6">
        <div class="card p-4 bg-light text-dark">
          <h3 class="fw-bold mb-3">Departments</h3>
          <p>View, add, update or delete departments easily.</p>
          <a href="/departments" class="btn btn-primary btn-lg">Go to Departments</a>
        </div>
      </div>
      <div class="col-md-6">
        <div class="card p-4 bg-light text-dark">
          <h3 class="fw-bold mb-3">Employees</h3>
          <p>Manage all employees and assign them to departments.</p>
          <a href="/employees" class="btn btn-success btn-lg">Go to Employees</a>
        </div>
      </div>
    </div>

    <footer class="mt-5">
      <p class="small">© <%= new Date().getFullYear() %> Office Management System</p>
    </footer>
  </div>
</body>
</html>
